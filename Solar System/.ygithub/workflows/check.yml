name: React + Vite CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '24'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🛠 Build the Vite app
        run: npm run build

      - name: ✅ Check if Vite dev runs (optional)
        run: npm run dev 
          